{
  "private": true,
  "name": "dynamic-item-global-state",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^2.0.0-beta.2",
    "purescript": "^0.14.0",
    "spago": "^0.19.1"
  }
}
