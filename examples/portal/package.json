{
  "private": true,
  "name": "portal",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^2.8.0",
    "purescript": "^0.15.6",
    "spago": "^0.20.9"
  }
}
